


//svg.addEventListener("click", function(e){
    //need current selected color in color picker
    //need current selected shape
    //need curent selected size
    //createShape(event, shape, size, color){
    //      let newLocation = identifyLocation(e) does the calculation for where to place the shape
    //      let newShape = buildShape(shape, size, color) creates the shape object and returns it
    //      placeShape(newShape, newLocation) creates the shape in that specific location
//}
//})